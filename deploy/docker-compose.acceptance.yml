# 验收专用 compose override
# 用途：将 postgres 端口映射到宿主 15432，供宿主侧 node 脚本和 psql 直连容器 PG
# 使用方法：
#   docker compose -f docker-compose.prod.yml -f docker-compose.acceptance.yml --env-file .env up -d --build
# 注意：此文件仅用于验收测试，不应在生产无监控环境长期运行
name: byok

services:
  postgres:
    ports:
      - "15432:5432"
